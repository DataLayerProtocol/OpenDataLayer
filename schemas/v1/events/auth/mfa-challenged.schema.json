{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://opendatalayer.org/schemas/v1/events/auth/mfa-challenged.schema.json",
  "title": "MFA Challenged",
  "description": "Fires when a user is presented with a multi-factor authentication challenge",
  "type": "object",
  "properties": {
    "method": {
      "type": "string"
    },
    "challengeType": {
      "type": "string",
      "enum": ["login", "sensitive_action", "step_up"]
    }
  },
  "required": ["method"],
  "additionalProperties": false
}
