{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://opendatalayer.org/schemas/v1/events/loyalty/reward_claimed.schema.json",
  "title": "Loyalty Reward Claimed",
  "description": "Fires when a user claims a loyalty reward.",
  "type": "object",
  "properties": {
    "rewardId": {
      "type": "string",
      "description": "The unique identifier of the reward."
    },
    "programId": {
      "type": "string",
      "description": "The unique identifier of the loyalty program."
    },
    "rewardName": {
      "type": "string",
      "description": "The name of the reward claimed."
    },
    "rewardType": {
      "type": "string",
      "enum": ["discount", "product", "gift_card", "experience", "shipping", "other"],
      "description": "The type of reward claimed."
    },
    "rewardValue": {
      "type": "number",
      "description": "The monetary value of the reward."
    },
    "pointsCost": {
      "type": "integer",
      "minimum": 0,
      "description": "The number of points required to claim the reward."
    }
  },
  "required": ["rewardId"],
  "additionalProperties": false
}
