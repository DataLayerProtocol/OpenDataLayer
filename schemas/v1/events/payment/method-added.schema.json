{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://opendatalayer.org/schemas/v1/events/payment/method-added.schema.json",
  "title": "Payment Method Added",
  "description": "Fired when a user adds a new payment method.",
  "type": "object",
  "properties": {
    "methodType": {
      "type": "string",
      "enum": ["credit_card", "bank_account", "paypal", "apple_pay", "google_pay"],
      "description": "The type of payment method added."
    },
    "last4": {
      "type": "string",
      "description": "The last four digits or characters of the payment method identifier."
    },
    "isDefault": {
      "type": "boolean",
      "description": "Whether this payment method was set as the default."
    }
  },
  "required": ["methodType"],
  "additionalProperties": false
}
