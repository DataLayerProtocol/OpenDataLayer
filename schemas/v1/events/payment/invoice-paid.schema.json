{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://opendatalayer.org/schemas/v1/events/payment/invoice-paid.schema.json",
  "title": "Invoice Paid",
  "description": "Fired when an invoice is successfully paid.",
  "type": "object",
  "properties": {
    "invoiceId": {
      "type": "string",
      "description": "Unique identifier for the invoice."
    },
    "amount": {
      "type": "number",
      "description": "The monetary amount that was paid."
    },
    "currency": {
      "type": "string",
      "pattern": "^[A-Z]{3}$",
      "description": "ISO 4217 3-letter currency code (e.g. USD, EUR, GBP)."
    },
    "paymentMethod": {
      "type": "string",
      "description": "The payment method used to pay the invoice."
    },
    "paidAt": {
      "type": "string",
      "description": "ISO 8601 datetime when the payment was made."
    }
  },
  "required": ["invoiceId", "amount"],
  "additionalProperties": false
}
