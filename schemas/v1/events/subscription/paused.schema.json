{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://opendatalayer.org/schemas/v1/events/subscription/paused.schema.json",
  "title": "Subscription Paused",
  "description": "Fired when a subscription is paused.",
  "type": "object",
  "properties": {
    "subscriptionId": {
      "type": "string",
      "description": "Unique identifier for the subscription."
    },
    "planId": {
      "type": "string",
      "description": "Unique identifier for the subscription plan."
    },
    "reason": {
      "type": "string",
      "description": "The reason the subscription was paused."
    },
    "resumeDate": {
      "type": "string",
      "description": "ISO 8601 date or datetime when the subscription is scheduled to resume."
    }
  },
  "required": ["subscriptionId"],
  "additionalProperties": false
}
