{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://opendatalayer.org/schemas/v1/events/order/processing.schema.json",
  "title": "Order Processing",
  "description": "Fired when an order enters the processing stage.",
  "type": "object",
  "properties": {
    "orderId": {
      "type": "string",
      "description": "Unique identifier for the order."
    },
    "status": {
      "type": "string",
      "enum": ["picking", "packing", "quality_check", "ready_to_ship"],
      "description": "The current processing status of the order."
    },
    "warehouseId": {
      "type": "string",
      "description": "Unique identifier for the warehouse handling the order."
    },
    "estimatedShipDate": {
      "type": "string",
      "description": "ISO 8601 date or datetime for the estimated ship date."
    }
  },
  "required": ["orderId"],
  "additionalProperties": false
}
