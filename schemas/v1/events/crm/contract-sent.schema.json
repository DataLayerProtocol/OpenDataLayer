{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://opendatalayer.org/schemas/v1/events/crm/contract-sent.schema.json",
  "title": "CRM Contract Sent",
  "description": "Fires when a contract is sent to a prospect for review and signature.",
  "type": "object",
  "properties": {
    "contractId": {
      "type": "string",
      "description": "Unique identifier for the contract."
    },
    "opportunityId": {
      "type": "string",
      "description": "Unique identifier for the associated opportunity."
    },
    "value": {
      "type": "number",
      "description": "Contract value."
    },
    "currency": {
      "type": "string",
      "pattern": "^[A-Z]{3}$",
      "description": "ISO 4217 3-letter currency code (e.g. USD, EUR, GBP)."
    },
    "expiresAt": {
      "type": "string",
      "description": "Date and time the contract offer expires."
    }
  },
  "required": ["contractId"],
  "additionalProperties": false
}
