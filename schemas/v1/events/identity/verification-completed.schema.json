{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://opendatalayer.org/schemas/v1/events/identity/verification_completed.schema.json",
  "title": "Identity Verification Completed",
  "description": "Fired when an identity verification process is completed.",
  "type": "object",
  "properties": {
    "verificationId": {
      "type": "string",
      "description": "Unique identifier for the verification request."
    },
    "verificationType": {
      "type": "string",
      "enum": ["kyc", "kyb", "age", "address", "phone", "email", "document", "biometric", "other"],
      "description": "The type of identity verification performed."
    },
    "provider": {
      "type": "string",
      "description": "The verification service provider."
    },
    "userId": {
      "type": "string",
      "description": "Unique identifier for the user who underwent verification."
    },
    "result": {
      "type": "string",
      "enum": ["approved", "denied", "pending_review", "inconclusive"],
      "description": "The outcome of the verification process."
    },
    "duration": {
      "type": "number",
      "description": "Time to complete verification in seconds."
    },
    "tier": {
      "type": "string",
      "enum": ["basic", "standard", "enhanced"],
      "description": "The verification tier or level that was completed."
    }
  },
  "required": ["verificationId"],
  "additionalProperties": false
}
