{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://opendatalayer.org/schemas/v1/events/social/post-deleted.schema.json",
  "title": "Social Post Deleted",
  "description": "Fires when a post is deleted.",
  "type": "object",
  "properties": {
    "postId": {
      "type": "string",
      "description": "The unique identifier of the deleted post."
    },
    "reason": {
      "type": "string",
      "enum": ["user", "moderation", "policy"],
      "description": "The reason the post was deleted."
    }
  },
  "required": ["postId"],
  "additionalProperties": false
}
