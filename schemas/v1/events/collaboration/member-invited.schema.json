{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://opendatalayer.org/schemas/v1/events/collaboration/member_invited.schema.json",
  "title": "Collaboration Member Invited",
  "description": "Fires when a member is invited to a workspace.",
  "type": "object",
  "properties": {
    "workspaceId": {
      "type": "string",
      "description": "The unique identifier of the workspace the member is invited to."
    },
    "inviteeId": {
      "type": "string",
      "description": "The unique identifier of the invited user."
    },
    "role": {
      "type": "string",
      "enum": ["owner", "admin", "editor", "viewer", "commenter", "guest"],
      "description": "The role assigned to the invited member."
    },
    "inviteMethod": {
      "type": "string",
      "enum": ["email", "link", "sso", "api"],
      "description": "The method used to send the invitation."
    }
  },
  "required": ["workspaceId"],
  "additionalProperties": false
}
